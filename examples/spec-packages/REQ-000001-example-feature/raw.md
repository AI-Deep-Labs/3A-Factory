# Raw — REQ-000001-example-feature

## Yêu cầu thô (mẫu)

Cần một API nội bộ đọc danh mục mẫu (categories) và trả JSON để UI demo hiển thị. Không đụng auth/production DB.

## Ghi chú

Package này chỉ là example documentation — không implement vào app thật.
