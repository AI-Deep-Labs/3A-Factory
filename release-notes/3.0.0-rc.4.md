# Release Notes — 3.0.0-rc.4

**Status:** Release candidate (pre-release). Version npm: `3.0.0-rc.4`. Final GA dự kiến: `3.0.0`.

## Tóm tắt

3A-Factory **3.0.0-rc.4** tiếp tục dòng candidate 3.0.0. Thay đổi chính so với [3.0.0-rc.3](./3.0.0-rc.3.md): canonical Spec Package path chuyển sang **`docs/tasks/`**.

## Thay đổi chính so với rc.3

- **Canonical Spec Package path:** `docs/tasks/REQ-<NNNNNN>-<slug>/` (không còn `.specs/`).
- Contract, skills, validators, governance docs đồng bộ path mới.
- Installer vẫn **không** tạo feature package dưới `docs/tasks/` (skills tạo khi triage).

## Spec Package / workflow

Xem [docs/workflow.md](../docs/workflow.md), [BREAKING-CHANGES.md](../BREAKING-CHANGES.md).

## Installation

```bash
npx 3a-factory@3.0.0-rc.4 --agent=cursor,gemini --force
```

Sau cài: Cursor Agent chat → `/`; Gemini → `/commands reload`.  
Package mới: `docs/tasks/REQ-…/` (di chuyển từ `.specs/` nếu còn package cũ).

## Release readiness

Theo `RELEASE-CHECKLIST.md` và `docs/reports/`. Publish RC chỉ khi user chủ động.
