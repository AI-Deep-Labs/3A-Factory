# Release Notes — 3.0.0-rc.3

**Status:** Superseded by [3.0.0-rc.6](./3.0.0-rc.6.md) (via rc.4–rc.5). Kept for history.

## Tóm tắt

3A-Factory **3.0.0-rc.3** (historical).
## Thay đổi chính so với rc.2

- Skill body **chỉ** trong `.agents/skills/` (bỏ mirror `.cursor/skills/` và `.gemini/skills/`).
- Cursor entry: `.cursor/rules/<skill>.mdc` + `ai-workflow.mdc`.
- Gemini entry: `.gemini/commands/*.toml` → `.agents/skills/`.
- **Canonical Spec Package path:** `docs/tasks/REQ-<NNNNNN>-<slug>/` (thay `.specs/`).

## Spec Package / workflow

Không đổi contract lõi. Xem [docs/workflow.md](../docs/workflow.md), [BREAKING-CHANGES.md](../BREAKING-CHANGES.md).

## Installation

```bash
npx 3a-factory@3.0.0-rc.3 --agent=cursor,gemini --force
```

Sau cài: Cursor Agent chat → `/`; Gemini → `/commands reload`.

## Release readiness

Theo `RELEASE-CHECKLIST.md` và `docs/reports/`. Publish RC chỉ khi user chủ động.
