# Release Notes — 3.0.0-rc.5

**Status:** Superseded by [3.0.0-rc.6](./3.0.0-rc.6.md). Kept for history.

## Tóm tắt

3A-Factory **3.0.0-rc.5** tiếp tục dòng candidate 3.0.0. Thay đổi chính so với [3.0.0-rc.4](./3.0.0-rc.4.md): **fix installer** — npm lifecycle không còn scaffold nhầm cả ba agent.

## Thay đổi chính so với rc.4

- **Fix installer:** npm `postinstall` / `install` / `preinstall` / `prepare` không scaffold project (`INSTALL_SKIPPED_LIFECYCLE`).
- Tránh `npx 3a-factory@… --agent=gemini` vô tình cài cả claude + cursor (default-all + `INIT_CWD`).
- Thêm regression tests giả lập luồng npx/postinstall.

## Spec Package / workflow

Không đổi so với rc.4: package dưới `docs/tasks/REQ-<NNNNNN>-<slug>/`.  
Xem [docs/workflow.md](../docs/workflow.md), [BREAKING-CHANGES.md](../BREAKING-CHANGES.md).

## Installation

```bash
npx 3a-factory@3.0.0-rc.5 --agent=gemini --force
```

Luôn truyền `--agent=…`. Sau cài: Cursor Agent chat → `/`; Gemini → `/commands reload`.

## Release readiness

Theo `RELEASE-CHECKLIST.md` và `docs/reports/`. Publish RC chỉ khi user chủ động.
