# Release Notes — 3.0.0-rc.6

**Status:** Release candidate (pre-release). Version npm: `3.0.0-rc.6`. Final GA dự kiến: `3.0.0`.

## Tóm tắt

3A-Factory **3.0.0-rc.6** sửa cấp số REQ/ADR: agent tự quét `docs/tasks/` và áp dụng `next = max + 1` (không có → `000001`). Thanh lọc example ô nhiễm (`REQ-000013`). **Không** dùng script allocator.

## Thay đổi chính so với rc.5

- Triage/ADR: agent liệt kê basename package/ADR thật; cấm lấy số từ `.agents/` / contract / skill body.
- Example trong tooling thống nhất `REQ-000001-…` / `ADR-000001-…`.
- Không ship `allocate-id.js` — agent tự xác định số thứ tự.

## Installation

```bash
npx 3a-factory@3.0.0-rc.6 --agent=gemini --force
```

## Release readiness

Theo `RELEASE-CHECKLIST.md`. Publish RC chỉ khi user chủ động.
